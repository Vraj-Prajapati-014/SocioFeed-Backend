const ACTIVITY_BASE_URL = '/activities';

// Error messages
const ERROR_NO_ACTIVITIES_FOUND = 'No activities found for this user';
const ERROR_UNAUTHORIZED = 'Unauthorized to perform this action';

// Pagination defaults
const DEFAULT_PAGE = 1;
const DEFAULT_LIMIT = 10;
const MAX_LIMIT = 50;

export const ACTIVITY_CONSTANTS = {
  ACTIVITY_BASE_URL,
  ERROR_NO_ACTIVITIES_FOUND,
  ERROR_UNAUTHORIZED,
  DEFAULT_PAGE,
  DEFAULT_LIMIT,
  MAX_LIMIT,
};
